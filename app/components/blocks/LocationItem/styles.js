import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  locationItem: {
    marginBottom: 12
  },
  description: {
    fontFamily: 'SFProText-Regular',
    fontSize: 17,
    color: '#000'
  }
})
